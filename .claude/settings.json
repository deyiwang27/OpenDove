{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command // empty'); case \"$cmd\" in 'git commit'*) uv run pytest tests/ -x -q ;; esac",
            "timeout": 120,
            "statusMessage": "Running tests before commit..."
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *.py) python -m py_compile \"$f\" ;; esac; } 2>/dev/null || true",
            "statusMessage": "Syntax-checking edited file..."
          }
        ]
      }
    ]
  }
}
